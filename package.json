{
  "dependencies": {
    "bluebird": "^3.7.2",
    "express": "^4.17.1",
    "node-fetch": "^3.0.0",
    "sqlite3": "^5.0.2"
  },
  "scripts": {
    "start": "node app",
    "server": "node server",
    "client": "npm start --prefix gm-app",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build --prefix gm-app"
  },
  "devDependencies": {
    "concurrently": "^6.3.0"
  }
}
